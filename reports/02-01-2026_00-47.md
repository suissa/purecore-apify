# Build Error Report - 02-01-2026_00-47

## Summary

Continuing the systematic resolution of TypeScript compilation errors in the `@purecore/purecore-apify` project.

## Progress Made

### Fixed Issues:

1. **`purecore-schemify`**: Fixed type mappings in `SchemifyArray`, `SchemifyObject`, and helper types for proper type inference with phantom properties.

2. **`purecore-syncify`**: Fixed `promisify` function overload signature compatibility by using `any[]` for the implementation.

3. **`native-multer`**: Removed incompatible `export = multer` statement that conflicted with ES module exports.

4. **`purecore-reqify`**: Fixed headers access and retryDelay non-null assertions in auto-healing logic.

5. **`auto-generator-ddd.ts`**: Fixed the missing `input` variable by correctly referencing `metadata.fields`. Added `any` type to catch blocks.

6. **`decorators/preset.ts`**: Updated `DecoratorFunction` type to accept `string | symbol | undefined` for `propertyKey`, matching TypeScript's `MethodDecorator` signature. Added existence check in iteration loop.

7. **`decorators/catch.ts`**: Moved `errorLog` to module scope so `getErrorLog()` and `clearErrorLog()` can access it. Fixed `error` type in catch blocks.

8. **`decorators/schema-validator.ts`**: Added `any` type annotation to all catch blocks.

## Remaining Errors (~226 lines)

### High Priority:

- **`src/decorators/config.ts`**: Multiple decorator type assignment errors due to `propertyKey` type mismatch
- **`src/decorators/resilience.ts`**: AsyncChannel null issues and void/Promise return type conflicts
- **`src/decorators/helmet.ts`**: Property 'value' access on potentially void descriptor
- **`src/decorators/memoization.ts`**: String | undefined assignments

### Medium Priority:

- **`src/libs/intent-router.ts`**: Array access possibly undefined (matrix operations)
- **`src/libs/semantic-healer.ts`**: Similar array access issues
- **`src/middlewares/`**: Multiple undefined handling issues
- **`src/packages/native-multer/`**: FileInfo to File type conversion issues
- **`src/packages/purecore-asyncify/`**: Controller and callbacks possibly undefined
- **`src/packages/purecore-syncify/`**: Parameter type mismatches in channel operations

### Lower Priority:

- Various `string | undefined` to `string` assignment issues
- Missing exports in some modules
- Implicit `any` type parameters

## Next Steps

1. Fix `DecoratorFunction` consumers in config.ts by updating their signatures
2. Resolve resilience.ts channel type issues
3. Add non-null assertions or guards to array/matrix operations
4. Handle FileInfo to File conversion in native-multer storage engines

## Technical Notes

- The project uses nominal/branded types (e.g., `Url` brand) which require explicit casting
- Many errors are cascade effects from core type definitions
- Focus should be on foundational types first, then downstream fixes
